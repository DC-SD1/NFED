/**
 * Shared constants for the grower application
 */

/**
 * Maximum count to display on badges before showing "+"
 * e.g., if count is 50 or more, show "50+"
 * Change this value to adjust the threshold for all cards (notifications, tasks, etc.)
 */
export const MAX_BADGE_COUNT = 50;
